#include <stdio.h>

/**
 * main - prints the number of arguments passed into it
 * @argc: argument count
 * @argv: argument arrays
 *
 * Return: 0 always
 */
int main(int argc, char **argv)
{
	(void)argv;

	printf("%d\n", agrc - 1);
	return (0);
}
